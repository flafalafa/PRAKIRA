"""Base middleware module."""
from starlette.middleware.base import BaseHTTPMiddleware

class PrakiraBaseMiddleware(BaseHTTPMiddleware):
    """Base class for all PRAKIRA custom middlewares."""
    pass
