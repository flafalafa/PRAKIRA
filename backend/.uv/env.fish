if not contains "$HOME/Documents/[2] Work SH/[4] PRAKIRA/PRAKIRA/backend/.uv" $PATH
    # Prepending path in case a system-installed binary needs to be overridden
    set -x PATH "$HOME/Documents/[2] Work SH/[4] PRAKIRA/PRAKIRA/backend/.uv" $PATH
end
